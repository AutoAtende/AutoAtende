import React from "react";
import WhatsMarked from "react-whatsmarked";

const WhatsMarkedWrapper = ({ children }) => {
	return <WhatsMarked>{children}</WhatsMarked>;
};

export default WhatsMarkedWrapper;
